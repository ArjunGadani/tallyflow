[tool.pytest.ini_options]
# Run from repo root; `backend` is an importable package.
pythonpath = ["."]
testpaths = ["backend/tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
filterwarnings = [
    "error::DeprecationWarning:backend.*",
]
